# child - minimal linker for x86_64 Linux

child は小さいです。childなので。

childは、x86_64 Linux (System-V ABI)において、
与えられた動的シンボルの呼び出しを含むいくつかのオブジェクトファイルに対して

- 静的リンク
- 動的リンクのためのGOT, PLT生成
  を行い、(動的リンクが必要な)実行可能ファイルを生成します。

要は、ただ静的リンクするだけじゃなくて、libcくらいは(動的に)使いたいよね、ということです。
